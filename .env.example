# Copy to `.env` and adjust for your environment. Never commit `.env` or API keys.
#
# Example (public read-only API root; replace with your backend when ready):
API_BASE_URL=https://www.themealdb.com/api/json/v1/1/
